<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/styles.css">
    <title>
        <%= article ? "Edit Article" : "New Article" %>
    </title>
</head>
<body>
    <header>
        <div class="button-area">
            <% if (isAuthenticated) { %>
                <form action="/logout" method="post">
                    <button class="button">logout</button>
                </form>
                <a href="/index.html">
                    <button class="button">back</button>
                </a>
                <% } %>
        </div>
        <div class="info-area">
            <h1 class="headline">
                <%= article ? "Edit Article" : "New Article" %>
            </h1>
        </div>
    </header>
    <main>
        <form action="<%= article ? '/update/' + id : '/new.html' %>" method="POST">
            <input required type="text" name="title" placeholder="Article title"
                value="<%= article ? article.title : '' %>">
            <input required type="text" name="date" placeholder="Publishing date"
                value="<%= article ? article.date : currentDate %>">
            <textarea required name="content" placeholder="Content"><%= article ? article.content : '' %></textarea>
            <button type="submit" class="button">
                <%= article ? "update" : "publish" %>
            </button>
        </form>
    </main>
    <footer>
        <h4>
            <%= year %> u-jin22
        </h4>
    </footer>
</body>
</html>